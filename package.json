{
  "name": "practice-tracker",
  "version": "1.0.0",
  "description": "Practice Tracker App for Wagner's School of Music",
  "main": "index.js",
  "scripts": {
    "clean": "rm dist/bundle.js",
    "start": "parcel public/index.html",
    "build": "parcel build public/index.html --out-dir build --no-source-maps --no-content-hash",
    "deploy": "npm run build && firebase deploy"
  },
  "keywords": [
    "firebase",
    "pwa",
    "react"
  ],
  "author": "Nathan Rubin",
  "license": "Apache-2.0",
  "dependencies": {
    "firebase": "^7.24.0",
    "react": "^16.14.0",
    "react-dom": "^16.14.0"
  },
  "devDependencies": {
    "less": "^3.13.1",
    "parcel-bundler": "^1.12.4",
    "prettier": "^1.19.1"
  }
}
